export interface User {
  auth: boolean;
  name: string;
  lastName: string;
  email: string;
  notify: boolean;
  role: number[];
  uid: string;
  identification: number;
  nationality: string;
  phoneNumber: string;
}
